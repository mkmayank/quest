----Installation-----
$ sudo apt-get install mitmproxy
$ LANG=C.UTF-8
$ mitmproxy --host
### set proxy to the 127.0.0.1:8080 and install certificate from mitm.it
$ mitmproxy --host # to monitor traffic

----------------------------
